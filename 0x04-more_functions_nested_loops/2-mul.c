#include "main.h"
#include <stdio.h>

/**
 * mul - mulptiplies two integers
 * @a: The first integer
 * @d: The second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
